# GuardLens — Python dependencies (HPC / SLURM cluster)
#
# Canonical install:
#   bash setup_guardlens_env.sh --recreate
#
# PyTorch is installed first by setup_guardlens_env.sh so the cluster can
# resolve the appropriate binary wheel. Keeping it here makes a direct
# `pip install -r requirements.txt` fail closed if torch is absent.
#
# Heavy evaluation backends such as vLLM and bitsandbytes are intentionally
# not installed by default because they may impose their own CUDA/Torch
# constraints. Install those only for the migrated evaluation jobs that need
# them.

# Core training / inference
torch>=2.2,<3
transformers>=4.48,<5
accelerate>=0.28,<2

# Numeric / evaluation utilities
numpy>=1.24,<3
scipy>=1.10,<2
requests>=2.31,<3

# Plotting
matplotlib>=3.7,<4

# Optional heavy evaluation dependencies (install only when the corresponding
# migrated evaluator is enabled):
#   bitsandbytes
#   vllm
